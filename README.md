Okay, let me say, I'm going to build a project. I am already building a project which is a Tamil Nadu, not specifically for Tamil Nadu, it's an electricity bill calculator, but defaultly it is for Tamil Nadu electricity bill calculator because I have a slab rates, but I don't have slab rates for other states. So I want like you do, ChatGPT, you have to write a prompt. It should be a clean prompt to cursor. I will give you the code to you and I will give you the instruction. You have to write the clean prompt to the cursor. Okay, let's start the instructions. I will attach a file with this chat. You can refer the code. Okay, let's begin. First thing is, in this code, the important thing is, don't change the UI of consumption history table. The consumption history table, it takes, it has a view, it is under your card, and we can call it as a consumption history card. This card will be taken, a screenshot will be taken by the canvas. which is HTML to Canvas library, and it will be sent through WhatsApp, okay? Already I have this feature, I can understand, but don't change the exact UI of the Canvas, of the card, consumption history card, okay? I want the exact UI. Don't change that. That's the important thing I have to mention because previous time you have changed the UI of that. Except this, you can change any UI, but don't change this part, okay? That's the first one. Second is, okay, uh, include Include base LMR. Base LMR. LMR means last month reading. Include base LMR column in the history table. This is the second thing. You have to update this thing because I don't have that column. So I want the base LMR column in the history table, okay? Yes, the third thing is, the value should be entered less than previous reading, which means if you are trying to enter the reading value, electricity reading value, you can enter, but it should be greater than the last updated reading. It should be greater than that. Okay, for example, if you, the last updated reading is in the consumption history table, you can see 1,500 units. Now we are trying to enter the today's current consumption reading, that is, which is 1,600 units, which should be more than 1,500, right? It should not less than 1,500. That's the thing, that's the third thing. Okay, that logic is not there. You have to create that logic. Then, okay, this is another important one. I want a structure to be, the consumption history table, entire consumption history table should be like a crud application, which is, you can read, yes, of course, but after you have to create the rows, yes, you can create, I already have, if you click the calculate button, you can create the rows with the data, but there is no edit functionality. I want, like, if I mistakenly entered some value, I have to edit that. I want, like, the last updated value should be edit, should be only for last updated value, not for the before that. Okay? The last updated value should be edit functionality. Now if I click edit, I can see the reading. I mean, I can edit the reading. I can edit the readings only, reading column only. Okay, nothing other than that I can't edit. And I can delete the row. This is the functionality I want for the consumption history table. It should be saved in the database and you can retrieve the data from the database every time I reload the page. Okay, like I already have a slab database, table like that. Okay, this is the fourth thing I think. Fourth thing, now fifth thing. The fifth point is the DB, slab database DB, slab database, I mean, sorry, slab database, you have the, I already have this slab database. I want like the max units should be, there are max units like 500 and 600 or 700 units maybe. I want like, it should be ordered like grouped by max units, max units. Okay, like, you know, it's showing like the from unit to unit, it is ascending to descending, it is ordered. But I want like, it should be ordered like, from 1 to 100 units, rate is zero, max unit is 500. Second row, it shows like 1 to 500 units, the rate is zero, the max unit is triple nine. It should not like, look like this. I want like, the second row should be 101. 200, 2.35, 500 units is the max units. Like this, you should show. After completing the max units group, you are sorting the... Finally, I am saying like, you have to sort, sort, s-o-r-t, s-o-r-t, sort by grouped max unit. Okay, you have to group by max unit. After that, you have to sort. This is the order I want, like, in this lab database. This is the fifth thing. Then, okay, next thing is, I have already have a functionality whether I can have the, I mean, like, if you want to take screen, I mean, canvas, the stream to canvas will take the snapshot and it will send through the WhatsApp. I have already have this functionality, but I want, like, the last updated, I mean, dates for the last, not like dates, it's like rows. The, how to say, the conservation history already is ordered in the descending order. It is in the reverse order. It's good, but I want, like, the first five rows, which is already reversed, right? So the last five updated things or the first rows, first five rows should be only sent through the WhatsApp, not more than that, because it will be not clear. So that I want, like, the first five rows or we can say, like, the last five updated readings. In the consumption history table, it can be sent through WhatsApp. Okay, this is the sixth thing. The seventh thing is reset and log. Reset log and send WhatsApp button. It is in the bottom of the consumption history table. It should be above the table because after adding more rows, I can't see that. I have to scroll down. So that I want these two buttons to be above the consumption history table. And again I am saying, don't change the UI of consumption history table. I want the exact UI with the yellow. Now it looks like yellow and with the black font and yes, it's very good. I don't want to change that.

Next functionality is you have to include the main thing is chart.js library to view the stats of the history table. I want chart.js to view the stats of the history table so that I can see the consumption history, unit readings, and the rate by the date. How much the bill amount. This is you want to build in the chart.js, that's the next functionality. Please include chart.js. Then delete unwanted libraries in my package.json because it's previously installed many unwanted packages. Delete unwanted packages and use the libraries like Typed.js, GSAP, Particle.js for graphics, animations, Typed.js to show the, to visualize anything, you can use this. Framer motions, okay? You can use these libraries in any of the UI, but except, again I'm saying, except you should not touch the consumption history table. You wait, okay, which is total units 500, will be in the black, I mean in the top of the consumption history table, it should not also be changed. You can include like, okay, next one, I will, next important thing is authorization. It should be user-based login. This is another main functionality. It should be user-based login. The data shows each user should have their own slab DB. Okay, each user should have their own slab DB, but by default, I have the slab rate now. That's the Tamil Nadu electricity board slab rate. I have already in my database, I have a slab rate, which should be default. But if the user wants to edit and add, they can do that in the authorization. You first, we have to find whether the user is authorized. So the first page should be contained a login page. After that, I can see the main EV dashboard page, which is home page. Okay, this is another main functionality I want. Okay, okay, ChatGPT, that's all. Another main, really, very important functionality, I missed to say this, very important functionality, which is the base LMR should be removed.

Okay, the last functionality, which is a very important functionality, which is the base LMR. You have to change this, right? The base LMR, just listen carefully. I'm going to say about this base LMR. Okay, the one lakh units is in the, it's just taken for example, I want a button, which is a reset button. When I click this reset button, what should happen is, it's a very dangerous button. It's like a very dangerous button. If you click this reset button, the readings, what you have, the latest reading in the consumption history, the reading will become the next base LMR. Can you understand this? Next base LMR. For example, I will say example, if the history, consumption history table has in the March 30, 2026, it has a reading of one lakh 500 units. The units is 500 and the bill amount, everything is there. After, if I try, if I, the EB in-charge people came to my home and changed their, if they take the reading and they will click the reset button there. And after that, I will click the reset button in my app. In this app, I will click the reset button in the danger zone. I will go to the danger zone in the left panel. I will click the edit button. I mean, I will click the reset button. I'm sorry, it is reset button. After clicking the reset button, the readings should get updated. Like what? The readings column will not get updated. The base LMR column will get updated. This column will get updated. How it will get updated means, if I put in the next entry, like 1,600 units, if I put 1,600 units, this is today's reading, I mean in the 31st March, now the row will be 31st March, 2026. After that, the reading is 1,600 units. The base LMR column will show 1,500 units this time. Previously, it shows 1 lakh units is the base LMR. Now it shows like 1 lakh 500 is the base LMR because I have clicked the reset button. After clicking reset button, it should show like this. This is the functionality I want. This is a very important functionality in my whole app. Okay. If I click reset, everything will get resetted and the previous reading, base LMR, the previous reading will become the current base LMR. So please write the code according to that. Okay.
